# Локальные изображения новостей

Файлы из этой папки доступны во frontend по адресу `/assets/news/<имя-файла>`.

Для новости про пекарню используется файл:

```text
bakery-fire.png
```

Для новости про общежитие используется файл:

```text
dormitory-missing.png
```
